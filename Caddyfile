{
  email admin@homecarepro.com.br
  on_demand_tls {
    ask http://homecarepro-web:3000/api/internal/caddy-ask
    interval 2m
    burst 5
  }
}

# Proxy dinâmico para todos os domínios via On-Demand TLS
https:// {
  tls {
    on_demand
  }

  reverse_proxy homecarepro-web:3000
  encode gzip
}
